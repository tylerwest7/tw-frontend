import project from "./project-schema";
import client from "./client-schema";
import portrait from "./portrait-schema";
import labs from "./labs-schema";

const schemas = [project, client, portrait, labs];

export default schemas;
